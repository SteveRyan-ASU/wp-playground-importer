# Playground ZIP Fixtures

Future integration-test fixtures for exported WordPress Playground ZIP packages belong in this directory.

Do not commit ad hoc exports. Every fixture should be deliberately selected or built for testing, reviewed for credentials, user data, environment-specific values, unnecessary media, and repository size before it is added.
